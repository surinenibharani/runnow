export type PlanDeletedMessage = {
  title: string;
  body: string;
};

export const PLAN_DELETED_MESSAGE: PlanDeletedMessage = {
  title: "Plan DNS — did not start (yet).",
  body: "Your schedule's on the bench with your post-run flip-flops. Zero shame in the recovery jog of life — lace back up when the legs (and the calendar) say go.",
};
